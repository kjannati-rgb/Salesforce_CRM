<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account__c</fullName>
    <label>Account</label>
    <type>Lookup</type>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Account Team Changes</relationshipLabel>
    <relationshipName>Account_Team_Changes</relationshipName>
    <required>false</required>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The account whose team membership changed. Always set by the controller; left optional so account deletion never cascades to or is blocked by the audit trail.</description>
</CustomField>
