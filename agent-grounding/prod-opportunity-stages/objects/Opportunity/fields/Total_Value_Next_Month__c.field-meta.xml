<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Value_Next_Month__c</fullName>
    <description>Total value next month (for NB&apos;s reports - do not delete)</description>
    <formula>IF(
AND(
MONTH(CloseDate) = MONTH(TODAY())+1,
text(Opportunity_Type_Forecast__c )= &quot;New Business&quot;
),
Amount,
0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Total value next month</inlineHelpText>
    <label>Total Value Next Month</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
