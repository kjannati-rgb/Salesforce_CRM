<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Order_Form_VAT_Tag__c</fullName>
    <description>Emits the Adobe signer-fillable VAT text tag ONLY when the account has no Sales Tax Number. Since 2026-08-23 the field is REQUIRED and shown only when the signer answers Yes to the VAT-registered question (showif). When known, the number prints as text and this field is blank.</description>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(Customer_VAT_Number__c), "{{*VAT_Number_es_:signer1:showif(VAT_Registered=Yes)}}", "")</formula>
    <formulaTreatBlanksAs>BlankAsBlank</formulaTreatBlanksAs>
    <label>Order Form VAT Tag</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
