<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Dispatch_Backstop_Hint__c</fullName>
    <description>REV-60 backstop UX: appended to the save-time Custom Error when a required Dispatch code is missing, so the rep knows what the valid values are (UAT round 1: a tester typed the product code into Dispatch to clear the block). Plain text, shown verbatim after the gap list.</description>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <label>Dispatch Backstop Hint</label>
    <length>255</length>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
