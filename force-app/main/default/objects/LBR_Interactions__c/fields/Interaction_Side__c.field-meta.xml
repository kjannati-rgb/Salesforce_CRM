<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Interaction_Side__c</fullName>
    <description>"Lead" or "Contact" depending on which side this interaction sits on. Drives conditional field visibility on the record page (REV-68); reference fields can't be used in visibility rules directly.</description>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(Lead__c), "Contact", "Lead")</formula>
    <label>Interaction Side</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
