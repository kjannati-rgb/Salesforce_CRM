<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_Form_Completion__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>REV-57 cross-link: points to an open PFC for the same person + same Product Brand. Populated by the orchestrator on the newer record; the reverse direction is surfaced automatically by the &quot;Linked Form Completions&quot; related list on the older record.</description>
    <label>Related Form Completion</label>
    <referenceTo>Pardot_Form_Completion__c</referenceTo>
    <relationshipLabel>Linked Form Completions</relationshipLabel>
    <relationshipName>Linked_Form_Completions</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
