<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Current_Deal_Length__c</fullName>
    <description>Combines two datasets to get the current deal length.</description>
    <externalId>false</externalId>
    <formula>IF(
AND(ISBLANK(
IF(
ISBLANK(SBQQ__PrimaryQuote__r.SBQQ__SubscriptionTerm__c),  Deal_Length__c, SBQQ__PrimaryQuote__r.SBQQ__SubscriptionTerm__c
)
),  
OR(TEXT(StageName) = &quot;Identify&quot;, TEXT(StageName) = &quot;Qualify&quot;, TEXT(StageName) = &quot;Evaluate&quot;, TEXT(StageName) = &quot;Contract Out&quot;, TEXT(StageName) = &quot;Closed Lost&quot;)
),
 Previous_Deal_Length__c,
IF(
ISBLANK(SBQQ__PrimaryQuote__r.SBQQ__SubscriptionTerm__c),  Deal_Length__c, SBQQ__PrimaryQuote__r.SBQQ__SubscriptionTerm__c
))</formula>
    <inlineHelpText>Combines two datasets to get the current deal length.</inlineHelpText>
    <label>Current Deal Length</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
