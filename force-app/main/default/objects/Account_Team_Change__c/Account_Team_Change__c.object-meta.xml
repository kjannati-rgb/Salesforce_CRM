<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>Immutable audit trail of Account Team Manager changes (add / remove / role change). Rows are written by AccountTeamManagerController in system context; actor and timestamp come from CreatedBy / CreatedDate. Public Read-Only so it is reportable; no edit/delete is granted, keeping the trail tamper-resistant.</description>
    <enableActivities>false</enableActivities>
    <enableHistory>false</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>false</enableSearch>
    <label>Account Team Change</label>
    <pluralLabel>Account Team Changes</pluralLabel>
    <nameField>
        <displayFormat>ATC-{00000}</displayFormat>
        <label>Change ID</label>
        <type>AutoNumber</type>
    </nameField>
    <sharingModel>Read</sharingModel>
</CustomObject>
