<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contact__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Contact this interaction belongs to. Optional: an interaction references either a Contact or a Lead (enforced by Contact_or_Lead_Required). Converted from Master-Detail to Lookup for REV-68 so pre-conversion interactions can sit on a Lead.</description>
    <inlineHelpText>The Contact this interaction belongs to. Leave blank if the interaction is on a Lead.</inlineHelpText>
    <label>Contact</label>
    <referenceTo>Contact</referenceTo>
    <relationshipLabel>LBR Interactions</relationshipLabel>
    <relationshipName>LBR_Interactions</relationshipName>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
